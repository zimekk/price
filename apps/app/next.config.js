/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // esmExternals: "loose",
  },
  transpilePackages: [
    // "@acme/auto",
    // "@acme/components",
    // "@acme/dyso",
    // "@acme/euro",
    // "@acme/hello",
    // "@acme/ross",
    // "@acme/xkom",
    // "@acme/ui",
  ],
};

module.exports = nextConfig;
